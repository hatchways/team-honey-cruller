import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  wrapper: {
    padding: '4rem 0px',
  },
  swiperContainer: {
    width: '100%',
    maxWidth: 500,
  },
}));

export default useStyles;
