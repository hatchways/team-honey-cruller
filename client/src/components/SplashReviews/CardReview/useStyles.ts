import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
    root: {
      height: '100%',
      width: '100%',
    },
    icon: {
      paddingBottom: '20px'
    }
  }));

  export default useStyles;