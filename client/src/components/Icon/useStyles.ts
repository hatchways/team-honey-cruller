import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
    extraSmall: {
        fontSize: 10,
    },
    small: {
        fontSize: 20,
    },
    medium: {
        fontSize: 30,
    },
    large: {
        fontSize: 40,
    },
}));

export default useStyles;