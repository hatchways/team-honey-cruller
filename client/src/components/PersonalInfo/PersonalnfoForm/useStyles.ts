import { makeStyles } from '@material-ui/core';

const useStyles = makeStyles(() => ({
  form: {
    padding: 10
  },
  textField: {

  },
  submit: {
    padding: 10,
    width: 200,
    borderRadius: 5,
    height: 60,
    fontSize: 14,
  },
  select: {
    top: 15,
  }
}));

export default useStyles;
