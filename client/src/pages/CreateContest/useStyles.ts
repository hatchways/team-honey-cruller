import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles(() => ({
  header: {
    fontWeight: 'bold',
  },
  paper: {
    marginBottom: 30,
  }
}));

export default useStyles;